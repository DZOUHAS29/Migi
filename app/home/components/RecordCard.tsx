
interface props {
    record: object
}

export const RecordCard: React.FC<props> = ({ record }) => {
    /* const { number, date, duration } = record;
     */
    return (
        <div>

        </div>
    )
}